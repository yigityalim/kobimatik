'use client';

import { PageLayout } from '@/components/marketing/page-layout';
import { menu } from '@/lib/menu';
import React from 'react';

export default function CompanyLayout({ children }: React.PropsWithChildren) {
    return <>{children}</>;
}
